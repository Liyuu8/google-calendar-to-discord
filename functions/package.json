{
  "name": "google-calendar-to-discord",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-functions": "^7.0.5",
    "googleapis": "^171.4.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "private": true
}